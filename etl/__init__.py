# etl/__init__.py
# Makes the etl/ directory a Python package so its modules can be imported as:
#   from etl.extract import fetch_all
#   from etl.transform import build_signals_df
#   from etl.load import save_all
